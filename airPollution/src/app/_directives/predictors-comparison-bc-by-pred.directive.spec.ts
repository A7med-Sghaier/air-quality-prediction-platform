import { PredictorsComparisonBcByPredDirective } from './predictors-comparison-bc-by-pred.directive';

describe('PredictorsComparisonBcByPredDirective', () => {
  it('should create an instance', () => {
    const directive = new PredictorsComparisonBcByPredDirective();
    expect(directive).toBeTruthy();
  });
});
