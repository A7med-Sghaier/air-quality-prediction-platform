import { PredictorsComparisonDirective } from './predictors-comparison.directive';

describe('PredictorsComparisonDirective', () => {
  it('should create an instance', () => {
    const directive = new PredictorsComparisonDirective();
    expect(directive).toBeTruthy();
  });
});
