import {WeatherChartDirective} from './weather-chart.directive';

describe('WeatherChartDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherChartDirective();
    expect(directive).toBeTruthy();
  });
});
