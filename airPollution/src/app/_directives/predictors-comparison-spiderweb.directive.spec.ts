import { PredictorsComparisonSpiderwebDirective } from './predictors-comparison-spiderweb.directive';

describe('PredictorsComparisonSpiderwebDirective', () => {
  it('should create an instance', () => {
    const directive = new PredictorsComparisonSpiderwebDirective();
    expect(directive).toBeTruthy();
  });
});
