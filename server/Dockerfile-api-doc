FROM node:8.17.0-buster AS docs-builder

ENV WORKDIR="/usr/src/app"

WORKDIR $WORKDIR

RUN npm install -g apidoc

COPY . $WORKDIR

RUN apidoc -i $WORKDIR -o /tmp/apidoc

FROM httpd:2.4-buster

COPY --from=docs-builder /tmp/apidoc /usr/local/apache2/htdocs
