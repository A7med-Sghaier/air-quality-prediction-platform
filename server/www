#!/bin/bash
set -e

API_PORT=8080
if [ ! -z "$PORT" ]; then
    API_PORT="$PORT"
fi

python - "$API_PORT" <<'PY'
import sys
from waitress import serve
from air_pollution.api.air_app import pandasApi

api_port = int(sys.argv[1])
serve(pandasApi, host='0.0.0.0', port=api_port)
PY
